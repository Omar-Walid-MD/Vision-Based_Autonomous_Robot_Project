# This file describes the .env file to create to configure the project for different platforms
# For each key, select one of the options in the list

PLATFORM=(WINDOWS | UBUNTU | RPI)
SERIAL_PORT=(COM-N | /dev/ttyxyz)
MAP_PATH="/path/to/map.json"